using UnityEngine;
using System.Collections;
using System.Runtime.InteropServices;

public class test_plugin : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}


	// Update is called once per frame
	void Update () {
        //SetTimeFromUnity(Time.time);
        //byte[] a = new byte[10];
        //OpenGL.TexSubImage3D(OpenGL.GL_TEXTURE_3D, 0, 0, 0, 0, 1, 1, 1, OpenGL.GL_ALPHA, OpenGL.GL_UNSIGNED_BYTE, ref a);
	}
}
